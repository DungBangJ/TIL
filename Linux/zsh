touch
- 파일 만들기
- 파일의 이름을 두고 공백을 입력하면 여러 파일을 한 번에 생성 가능하다.

mkdir
- 폴더 만들기
- 파일의 이름을 두고 공백을 입력하면 여러 파일을 한 번에 생성 가능하다.
- \을 사용하면 한 폴더의 이름에 공백을 줄 수 있다.

rm
- 파일 지우기
- 공백을 둬서 여러 개도 삭제 가능하다.
- rm -r
  - 폴더에 있는 모든 파일을 지운다.

rmdir
- 폴더 지우기
- 공백을 둬서 여러 개도 삭제 가능하다.
- \을 사용해야 이름에 공백을 둔 폴더를 삭제할 수 있다.

